# bind-attach-fix 測試報告

- 時間(UTC): 2026-02-11 08:22:29
- 版本: v1.1.1-serialwrapd-test
- 變更:
  - 新增 `session bind` RPC/CLI（執行期重綁 device_by_id）
  - bind 持久化到 `/tmp/serialwrap/state.json`（bindings）
  - 啟動 attach 改背景執行，避免 daemon socket 延後建立
  - 修正 UART speed 設定跨平台 `AttributeError` 問題

## 測試
### 1) 單元測試
```bash
PYTHONPATH=/home/paul_chen/arc_prj/ser-dep /usr/bin/python3 -m unittest discover -s /home/paul_chen/arc_prj/ser-dep/tests -p "test_*.py" -v
```
結果: `Ran 13 tests ... OK`

### 2) 實機流程（沙盒外）
命令序列:
- daemon start
- session bind COM0 -> `/dev/serial/by-id/usb-FTDI_FT232R_USB_UART_AC01QZT0-if00-port0`
- 輪詢 session state

觀察:
- bind 立即回 `state=ATTACHING`（非阻塞）
- 約 10 秒後轉 `DETACHED`，`last_error=PRPL_PROMPT_TIMEOUT`
- 代表 attach 鏈路與權限可用，阻塞點為 target 未出現 prpl prompt（非 bind/daemon 邏輯錯誤）
