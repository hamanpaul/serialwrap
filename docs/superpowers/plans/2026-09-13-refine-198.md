# #198 UART 收斂實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans；本次由 root 唯一派工及審查，實作者不得自行派 subagent。

**Goal:** 交付 #198 已批准的 ownership 補強、#166/#199 修復、#171 CLI trace 及後續驗證安排。

**Architecture:** 延用現行 broker、SessionManager 與 RPC client，不引入第二套 ownership 或硬體狀態。四個獨立 worktree 並行，root 合併共享檔案並驗證。

**Tech Stack:** Python 3.10+、pytest、現有 UART fake/PTY、stdlib logging；無新依賴。

**Spec:** openspec/changes/refine-uart-ownership-diagnostics/design.md 與各 task 指定 capability spec；盤點 docs/refine/198-ownership-audit.md、198-diagnostics-plan.md。

## Global Constraints

- 所有文件／註解／commit 使用繁體中文；README 由 root 做中英同步。
- 只可在已指定 worktree／branch 改檔。不得 git pull/push/merge、不得改其他 worktree、不得自行建立 PR／修改 issue，root 統一交付。
- 不新增依賴、不重構無關模組、不改 VERSION、不改政策檔，不啟停安裝 daemon、不讀寫真實 UART 或 live config/state/WAL。
- 所有檔案編輯使用 apply_patch；production change 前先新增 behavioral test 並實際觀察正確 RED。測試使用 pytest，不用 unittest；保留 conftest 的 production live guard。
- 先定向 pytest，完成後 python3 -m pytest -q tests/ 及 python3 -m policy_check --repo .；原有警告不冒充新失敗，失敗查因不得忽略。
- 每 task 新增唯一 changelog.d fragment 及 docs/refine 專屬結果文件；不改共享 README，root 整合。
- Commit 使用 Conventional Commits 繁中 subject，trailer 為 Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>。
- 不派任何子 agent／reviewer。root 另派 Opus xhigh 與 Sol xhigh。請回報真實 RED/GREEN 命令、結果、commit、未驗證項，不能以文件代替實作或假稱實機通過。
- 缺陷判準：未處置的缺陷／缺口為 FAIL；明文承認、影響有界且列管的殘餘風險不單獨 FAIL。不准把已重現的安全競態僅改文件放行。

## 派工前交叉檢查

| Task 配對 | 共享檔案 | 邊界與整合方式 |
|---|---|---|
| 1 / 2 | session_manager.py 的 file_push/pull | 1 只增加傳輸參數，2 只變更 admission／operation 身分；root 逐 hunk 合併 |
| 1 / 4 | session_manager.py | 4 只修改 _probe_external_holder，無重疊函式 |
| 2 / 4 | session_manager.py | 2 不動 holder；4 不改 admission |
| 3 / 其他 | 無 production 共寫 | CLI/client/new trace 模組只有 task 3 |
| 所有 | README、OpenSpec tasks | 僅 root 修改；各自 docs/refine 與 fragment 唯一檔 |

四項可獨立 review／reject；規格值、函式名稱與錯誤碼於各項定義，不依賴其他未合併 worktree。所有測試資料由 temp fixture 提供，不用真板作 baseline。

### Task 1: #166 雙向工具 fallback 與行長預算（Claude Sonnet xhigh）

**Files:** sw_core/file_transfer.py、sw_core/config.py、sw_core/session_manager.py（僅 file_push/pull 參數）、tests/test_file_transfer*.py、tests/test_config_profiles.py、regression/serialwrap_regression/cases/f07_file_transfer.py；新增 tests/test_transfer_portability.py、docs/refine/198-transfer-delivery.md、changelog.d/166-transfer-portability.md。可新增 config 測試檔；不得碰其他 manager admission。

**Spec:** openspec/changes/refine-uart-ownership-diagnostics/specs/uart-transfer-portability/spec.md。另讀 issue #166 最新 comment（gh issue view 166 --repo hamanpaul/serialwrap --comments）、docs/regression-plugin.md，避免沿已推翻的 timeout 假說。

**Interfaces:** 增加 frozen profile 可選 max_console_line_chars: int | None = None（正整數或 None，bool 不算 int）；命令 UTF-8 byte 長度不含 LF。push_file/pull_file 接受同名 keyword，SessionManager 傳 profile 值；未設定維持既有 DEFAULT_CHUNK_SIZE=512。實際 chunk 是使用者 chunk 上限與預算可容納 bytes 的較小值；至少扣除較長 append 模板及實際 decoder。對每個 probe、md5、mv、cleanup 等固定命令也驗長，無法滿足在 TX 前拒絕。TARGET_DECODER_MISSING／TARGET_ENCODER_MISSING 表達工具缺失，CONSOLE_LINE_LIMIT_TOO_SMALL 表達合法但太小預算。不得依 platform 名稱自動填 505。

- [ ] Step 1 RED：建立能模擬 command echo、shell 工具能力、rx snapshot 的 fake transport；需要時用真正 subprocess shell 驗證 OpenSSL fallback binary/empty，不只比對產生字串。至少 assertions：全部工具缺失時回 TARGET_DECODER_MISSING 且無資料／mv；echo 中出現 probe 文字不能成功；僅 OpenSSL 的 push/pull checksum 正確；505 budget 各送出命令 len(cmd.encode()) <= 505；quoted remote path、too-small budget 0 TX；invalid profile 解析拒絕。
- [ ] Step 2：python3 -m pytest -q tests/test_transfer_portability.py，保存實際 RED（missing keyword／缺 fallback／超長 TX，非 fixture 錯誤）。
- [ ] Step 3：最小實作以實際成功 sentinel 驗證 base64/openssl 編解碼，不從 command echo 判定；計算 (budget - max_shell_overhead) // 4 * 3 的 raw 上限並驗完整命令。維持 echo stall cancel_input_line 先清半行再 cleanup、checksum、ack_mode 契約。探測錯誤與 timeout 不能誤標成單純缺工具；工具後續失效不得只靠 prompt 判成功。修正既有假 bridge 以正確模擬新 probe，不刪舊 assertions。
- [ ] Step 4：定向測試加原 file_transfer/echo/config tests，再全套 pytest/policy。評估 f7-binary-roundtrip-md5／f7-larger-file-not-truncated，讓 fallback 有真正驗證入口，不因只缺 base64 提前 SKIP；issues 掛 166。不得實際跑真板。
- [ ] Step 5：專屬文件記設定例、錯誤碼、probe 成本與未驗證真板項；完成 fragment、commit、報告。

### Task 2: Ownership 決定性補強及重現競態修復（Codex gpt-5.6-luna max）

**Files:** sw_core/session_manager.py（admission、operation/epoch cleanup）、必要時 sw_core/arbiter.py、sw_core/uart_io.py；新增 tests/test_refine_ownership.py，必要修改既有 interactive/recovery/transfer 測試；docs/refine/198-ownership-delivery.md、changelog.d/198-ownership.md。不改 transfer 演算法／profile 欄位／holder。

**Spec:** openspec/changes/refine-uart-ownership-diagnostics/specs/uart-ownership-evidence/spec.md；讀 docs/refine/198-ownership-audit.md。正常 queue 多 client accepted；interactive_id 是 handle 不是 client ACL。file.push/pull 目前直接 manager，不經 arbiter；foreground_busy 只有設定，須驗 admission。unregister join timeout 舊 callback 仍活著，別只看一 worker 即判安全。

**Interfaces:** 保持 public API 及正常 queue 契約。busy admission 沿現有 SESSION_BUSY（先查既有 code）；同 manager lock 下獲取 session/bridge 身分，競爭 loser 不 TX。操作 finally 必須只釋放自己那一代的狀態；不得清新 session/bridge 的 busy。若既有 bool 無法辨身分，增加 internal operation token／guard，不公開 token API。

- [x] Step 1：建立 Event/barrier 停住第一個 transfer；另一 push/pull/foreground command 到達，assert loser.busy 且 bridge writes 不增加。逆方向 command 正執行時 transfer 同樣不寫。用換 bridge／re-register 時間點測舊 callback 不能寫新 bridge，也不能清掉新 operation busy；測试不可用 sleep 猜競態。
- [x] Step 2：python3 -m pytest -q tests/test_refine_ownership.py，保存真正行為 RED；若某案例本已通過，保留為 characterization，不能假造 RED。
- [x] Step 3：僅修已重現缺陷。lock 內先拒絕既有 busy，再標記本 operation；完成及例外清理用身分判斷，避免 epoch 跨代。長操作不持 manager lock 阻塞其他 session；同時覆蓋 human suspend/resume、FLASHING、background 與 interactive admission，確認沒有新 writer 旁路。
- [x] Step 4：補 unknown/closed/expired/replaced ID 0 TX、secondary console raw 不直送而 newline 經 broker、grace 內重連保留／逾 grace 舊 ID 拒絕及新 ID 可用。正反 assertion 使用 real manager/UART bridge fake boundary，不只 Mock 調用驗證自身。跑定向現有 arbiter/recovery/interactive/transfer tests，再全套 pytest/policy。
- [x] Step 5：文件記 exact test、修復根因、保證界線、剩餘 residual 與下一驗證；fragment、commit、報告。

### Task 3: #171 最小 CLI trace（Copilot gpt-5.4 xhigh）

**Files:** sw_core/cli.py、sw_core/client.py、可新增 sw_core/cli_trace.py；tests/test_cli_diagnostics.py、必要 CLI/client tests；docs/refine/198-cli-trace-delivery.md、changelog.d/171-cli-trace.md。不得改 daemon/service/manager/rpc_posix 或啟動真 daemon。

**Spec:** openspec/changes/refine-uart-ownership-diagnostics/specs/cli-rpc-trace/spec.md 與 docs/refine/198-diagnostics-plan.md（整份是本 task 詳細契約）。

**Interfaces:** rpc_call 回應 dict 完全不變，optional internal trace sink/metadata 取得 last-main-attempt errno、retry_count。單次解析提供 endpoint 與 source，原 _resolve_endpoint 介面保留相容 wrapper。_run_rpc、event 與 daemon start 原有 CLI RPC 呼叫都沿同一 trace wrapper；原本無 RPC 的子命令不加 RPC。trace 白名單正好 endpoint_transport,endpoint_id,endpoint_source,method,elapsed_ms,error_code,errno,errno_name,retry_count,timeout_s。ID 採 SHA-256 完整 64 hex，TCP literal loopback 可顯示 host+port。專用 logger serialwrap.cli_trace propagate=False，冪等自有 handler，不影響 root/serialwrap logger。-v INFO/-vv DEBUG > 有效 SERIALWRAP_LOG_LEVEL > WARNING；不認得 env 安靜回 WARNING。INFO trace。

- [x] Step 1 RED：tests/test_cli_diagnostics.py fake AF_UNIX/TCP server 或 transport boundary，驗 default stdout/stderr byte compatibility、verbose單一trace、repeated main reset；測 ENOENT/ECONNREFUSED/EACCES，retry 後 success errno null、TIMEOUT enrich 的 OSError 不污染主請求、event.rule_set 真正 method、config fallback probe count 相同、secret/params/raw endpoint 缺席、logger 隔離。
- [x] Step 2：python3 -m pytest -q tests/test_cli_diagnostics.py，保存 missing -v／trace 的 RED；既有 #172 error assertions 不刪不改。
- [x] Step 3：最小 internal metadata 通道與一次 endpoint resolution，logging 使用白名單 JSON stderr；monotonic 從主 rpc_call 入口至最終回應，含 retry/backoff/enrich。讀-only retry 維持原 whitelist/次數，mutating RPC 不新增 retry。errno 捕捉 OSError.errno 不解析 message，不增加 stdout 欄位。
- [x] Step 4：定向新測試與 tests/test_cli_stderr_full_error.py、既有 client/CLI/endpoint/event tests，再 full pytest/policy。trace on/off 必須有完全相同 RPC 序列，不能新增 health probe、lease touch、self_test 或 UART TX。
- [x] Step 5：文件記 trace 範例（不含 secrets）、flags/env precedence、TIMEOUT 不代表未執行、hash 非強匿名、#171 daemon logging 後續；fragment、commit、報告。

### Task 4: #199 缺 st_rdev 的 holder 探測修復（agy gemini-3.8-flash high）

**Files:** sw_core/session_manager.py 僅 _probe_external_holder；新增 tests/test_holder_probe_portability.py；docs/refine/198-holder-delivery.md、changelog.d/199-holder-stat.md。不得碰其他 manager 函式。

**Spec:** openspec/changes/refine-uart-ownership-diagnostics/specs/holder-probe-portability/spec.md；gh issue view 199 --repo hamanpaul/serialwrap。

**Interfaces:** _probe_external_holder 既有回應形狀不變。兩處 os.stat(...).st_rdev 以 getattr(os.stat(...), 'st_rdev', 0) 取得；0 不視為可做裝置同一性證據。保留 real_path/readlink 相等判斷及正常 POSIX char-device 比對。

- [x] Step 1 RED：實際呼叫函式，mock 僅 os.stat/listdir/readlink 邊界，缺 endpoint st_rdev 與缺 fd st_rdev 各一案例，原版須 AttributeError；另測正常 rdev 相等可找到 holder、不同 rdev 不誤判、同 path 仍匹配、無 /proc 安全空結果。
- [x] Step 2：python3 -m pytest -q tests/test_holder_probe_portability.py，保留 RED output。
- [x] Step 3：最小兩處 getattr fallback。不要聲稱無 /proc 是 Windows exclusive；不增加探測或依賴。
- [x] Step 4：定向新測試及既有 holder/flash 測試，再 full pytest/policy。
- [x] Step 5：文件明列 mocked seam 與實際 Windows 的差異；此缺屬 pytest 覆蓋，實際外部 flasher斷線為既有真機回歸，不假稱本輪實測。fragment、commit、報告。
